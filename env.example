# Copy this file to `.env` (same directory where you run `ecs`), then edit values.
#
# Alibaba Cloud credentials (recommended)
ALIBABA_CLOUD_ACCESS_KEY_ID=your_access_key_id
ALIBABA_CLOUD_ACCESS_KEY_SECRET=your_access_key_secret

# Optional: put the state file in a synced location
# ECS_STATE_FILE=D:\sync\ecs_state.json

# Optional: machine-specific SSH key path override (without touching JSON config)
# ECS_SSH_KEY=C:\path\to\your.pem

# Optional: explicit .env path (if you don't want auto-discovery)
# ECS_ENV_FILE=C:\path\to\.env


